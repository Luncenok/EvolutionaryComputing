#ifndef GREEDY_REGRET2_H
#define GREEDY_REGRET2_H

#include <vector>

std::vector<int> greedyRegret2(int startNode, int selectCount, const std::vector<std::vector<int>>& distance, const std::vector<int>& costs);

#endif
