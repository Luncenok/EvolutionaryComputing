#ifndef CALCULATE_OBJECTIVE_H
#define CALCULATE_OBJECTIVE_H

#include <vector>

int calculateObjective(const std::vector<int>& solution, const std::vector<std::vector<int>>& distance, const std::vector<int>& costs);

#endif
